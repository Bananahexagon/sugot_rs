#raw_js(
    const sugot_println = console.log;
)#

extern fn println(s: string): void

fn main(): void {
    let i: int = 0;
    let v: [int] = [2,4,6,8,10];
    while i != 5 {
        ((v[i]) as string).println();
        i = i + 1;
    }
}